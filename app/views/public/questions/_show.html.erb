
    <%= link_to public_quiz_question_path(quiz.id,question.id, step: "next"),class:"#{answer.id} double d-flex align-items-center justify-content-center",remote: true do %>
      <%= answer.body %>
    <% end %>
    <input type="hidden" id = "answer" value= '{"answer_id":<%= answer.id %>,"question_id":<%= question.id %>}'>
